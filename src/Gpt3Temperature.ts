/** Temperature for GPT-3 completion */
export enum Gpt3Temperature {
    /** Normal temperature with high creativity. */
    Default = 0.9,
    /** Lower temperature with less freedom. */
    Stricter = 0.6,
}